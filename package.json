{
  "name": "blog",
  "version": "1.0.0",
  "description": "Barebones blog written in Deno which generates SSR static pages.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "compile": "node compile.js",
    "bundle": "npm run compile && deno run --allow-run --allow-read=$(pwd) --allow-write=$(pwd) --import-map=./import_map.json build.tsx",
    "start": "deno run --import-map=import_map.json --watch  --allow-net --allow-read=$(pwd) server.tsx"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mdx-js/mdx": "^2.0.0-rc.2",
    "fastify": "^4.0.1",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "rehype-highlight": "^5.0.2",
    "remark-gfm": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.42"
  }
}
